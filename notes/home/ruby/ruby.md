```sh
bundle init (will create gemfile, we also need main.rb file)
bundle install (install dependencies)
bundle exec ruby publish.rb (running the publisher)
bundle exec ruby listen.rb (running the subscriber)
```