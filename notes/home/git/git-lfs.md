# LFS = Large File Storage
It's a Git extension for handling large files (videos, datasets, binaries, ML models etc.) that are too big for regular Git repositories. 
Instead of storing the actual file in Git, it stores a small pointer file and the real content is uploaded to a separate LFS server.
